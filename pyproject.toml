[project]
name = "data-analysis-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "pandas>=3.0.3",
    "redis>=5",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
